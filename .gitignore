node_modules/
*.log
.DS_Store
Thumbs.db
scratchpad/
*.output
.wrangler/
.dev.vars
.env
*.local
.claude/scheduled_tasks.lock

# audit intermediates — regenerable from the worklist, and noisy in diffs
data/_gf_audit_shards/

# agent scratch space — fetched pages, working briefs; never a deliverable
tmpwork/
