DEEPLY CONNECTED DINING — PROJECT HANDOFF
深い関係食事 · for a fresh Claude. Last updated 2026-06-27.
================================================================
WHAT THIS IS
A curated GF/vegan/cultural-depth dining guide PWA for Japan (Greg + students; July trip).
Single-file app + map + survival tools. Companion project: FKTI Learning (kana trainer) at
C:\Users\FKTI Work\Downloads\fkti-learning (SEPARATE repo + site — see its own HANDOFF.txt).

LIVE: https://fkti-dining-app.pages.dev   (Cloudflare Pages; note the "-app").
  ⚠ An OLD project https://fkti-dining.pages.dev (no "-app") is stale — ignore/retire it.
  DCP authoring tool: https://fkti-dining-app.pages.dev/dcp-tool

DEPLOY — GREG DEPLOYS HIMSELF. He runs:  cd into repo && npm run deploy  (wrangler pages deploy .)
  Service worker precaches the shell → BUMP `const VERSION` in sw.js every time index.html changes
  (currently dcd-v81). Data files are runtime-fetched (no bump needed for data-only changes).

STANDING RULES (do not violate)
- Greg DEPLOYS himself — never run a deploy.
- No paid/extra-usage tokens when unattended; included quota only; no retry/auto-resume crons.
- I never handle credentials/secrets; Greg sets them.
- Celiac safety is #1 — never assert a dish/place is safe; give honest GF/vegan reality + "ask".
- Do NOT bundle proprietary/copyrighted assets; open licenses + attribution only.

BUDGET CONTEXT (2026-06-27): Greg used ~69% of weekly tokens in the first ~26h. Favor LOCAL/cheap
work (single-file edits, OSM pulls, authored content). DEFER token-heavy multi-agent runs.

STRUCTURE
- index.html ........ the whole app (HTML/CSS/JS). Manifest-driven city registry + Leaflet map.
- sw.js ............. service worker (VERSION bump on shell changes).
- functions/api/menu-scan.js, label-scan.js ... Cloudflare Functions, Mistral Pixtral vision.
- data/manifest.json ... city registry {version,default_city,cities:[{id,name,file,center,bounds,free}]}.
- data/<city>.json ..... per-city {city,subtitle,center,bounds,tz,places:[...]}.
- data/<city>_konbini.json {city,updated,points:[{lat,lng,c,nm,addr,hours}]}  c=seven|family|lawson|...
- data/<city>_grocery.json {city,updated,stores:[{lat,lng,chain,name,addr,hours}]} (Kyoto richer: gf_level/summaries)
- data/menupedia.json ... {city,hundred:[100 dishes],lore:{key:{history,culture,gf,vegan}}}.
- data/_dishbank_canonical.json, _menu_kyoto.json, kyoto_menus.json ... source dish/menu data.
- scripts/gen-spot-workflow.mjs, build-spot.mjs, spot-configs.mjs ... city-sweep tooling (TOKEN-HEAVY).
- MOBILE_OPS.md ... mobile/GitHub-deploy playbook.

CITIES (8): kyoto(default), kanazawa, nara, hiroshima, nagano, toba, nagoya — all with restaurant
  data — plus TOKYO (added 2026-06-27 as a KONBINI/SURVIVAL city only: places:[] + friendly subtitle;
  restaurant guide deferred). OWN_ALL=true so all cities are unlocked.

FEATURES (current)
- Map of curated places; four-pillar pills (Vegan/GF/Ramen/Mom&Pop, combinable); filter sheet
  (celiacOnly/hideDiet/cuisine); Build-a-Bowl; per-place detail with DCP cultural notes.
- 🏪 Konbini + 🛒 Grocery SURVIVAL LAYERS in every city incl. all of Tokyo (konbini 5,910; grocery
  2,425). Map FAB toggles. Renderer is VIEWPORT-CULLED (drawSurv: only in-view, cap 1500/800) so big
  cities stay smooth. Konbini dot → per-store view (address/hours + chain allergen write-up + label
  reader). Grocery dot → per-store research (Kyoto) OR chain-keyed GF/vegan KB (GROCERY const:
  Seijo Ishii/Gyomu/KALDI/AEON-Topvalu/Life/natural-food/…) + label reader.
- 🍱 100 Dishes (menupedia) — toolbar button → all 100 top dishes (ja/romaji/en + #shops + ★), tap →
  full History+Culture reader with GF/vegan reality. ALL 100 readers authored.
- 📚 Learn — in-app survival-Japanese flashcards (separate from the kana trainer).
- 📷 Menu scanner + ingredient/label reader (Mistral Pixtral). ⚠ NEEDS Greg to set MISTRAL_API_KEY in
  the fkti-dining-app Cloudflare project (Settings → Env vars) — until then scanners return wired:false.
- DCP authoring tool (dcp-tool.html) — field-ready, with photo support; build-dcp.mjs renders notes.

RECOVERY LESSON: Workflow .output writes only at completion — a 0-byte .output ≠ stalled. Don't restart
  a running sweep (lost tokens once). If a sweep dies, HARVEST completed results from the transcript
  (subagents/workflows/<runId>/agent-*.jsonl, tool_use name==='StructuredOutput') instead of re-running.

TODO — CHEAP (do anytime, local logic)
- Learning app "Words" module (separate repo) when Greg sends lists.
- More menupedia readers can be deepened by editing data/menupedia.json lore{} (no app change).
- Per-store deep grocery GF research is the ONLY way to enrich non-Kyoto grocery (token-ish, optional).
- UI polish; bug fixes (e.g., watch for diet false-positives like the past Oyaji-under-vegan issue).
- Image-hosting: move menu/owner/DCP images to URL-referenced static files (Pages/R2), not base64.

TODO — TOKEN-HEAVY (DEFER to budget reset / small mobile bites during the trip)
- Restaurant sweeps for TOKYO (small radii), HIMEJI (castle↔station), TOBA Okage Yokochō near Ise.
  Use gen-spot-workflow.mjs / build-spot.mjs / spot-configs.mjs (Haiku discover → Sonnet verify →
  Opus safety-recheck of gf/vegan). These spin up many subagents — only with Greg present + budget.
- Per-store grocery GF research across cities (multi-agent) — deferred.
- Opus rechecks of existing ramen (e.g., GYUMON/Wagyu cleanup in Kyoto) — small, targeted only.

GREG MUST DO: set MISTRAL_API_KEY on the fkti-dining-app Cloudflare project to light up the scanners.
